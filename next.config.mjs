/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/AulaFinder',
  images: {
    unoptimized: true,
  },
  trailingSlash: true,
};

export default nextConfig;
