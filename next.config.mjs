/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/Aula-Finder',
  images: {
    unoptimized: true,
  },
  trailingSlash: true,
};

export default nextConfig;
